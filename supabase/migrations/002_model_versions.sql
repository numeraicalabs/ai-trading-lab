-- ============================================================
-- Migration 002 — no-op (already included in 001_init.sql v3)
-- This file is kept for reference only. Nothing to run.
-- ============================================================
select 'Migration 002: nothing to do — all tables already in 001_init.sql' as result;
